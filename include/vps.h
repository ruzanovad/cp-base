// #ifndef cp_base
// #define cp_base

#include <cmath>
#include <string>
#include <vector>

using namespace std;

namespace vps {
bool is_valid(string& s);
int depth(string& s);

bool is_valid_with_fillings(string& s);
}  // namespace vps

// #endif  // cp_base